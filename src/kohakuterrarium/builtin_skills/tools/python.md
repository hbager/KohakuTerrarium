---
name: python
description: Deprecated name for the run_python tool
category: builtin
tags: [code, execution, interpreter, deprecated]
---

# python

The `python` tool was renamed to `run_python` and is no longer registered.
Use `run_python` with the same `code` and optional `timeout` arguments.
