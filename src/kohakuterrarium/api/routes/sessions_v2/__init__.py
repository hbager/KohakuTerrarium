"""Sessions v2 routes — engine-backed creature ops (Phase 0 stubs).

Named ``sessions_v2`` (not ``sessions``) so it can coexist with the
legacy ``api/routes/sessions.py`` module during the cutover. Phase 1
will populate the stubs and Phase 2 will drain the legacy file.
"""
