"""Studio identity — configuration state (LLM, keys, MCP, codex, prefs).

This package centralizes reads, validation, and writes for identity
configuration. CLI and HTTP adapters delegate here instead of parsing YAML or
JSON independently.
"""
